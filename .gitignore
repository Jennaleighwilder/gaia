# JavaScript (any subtree — never commit dependencies)
node_modules/

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.Python
.pytest_cache/
.mypy_cache/

# IDE
.idea/
.vscode/
*.swp
*.swo

# CDS retrieve staging (avoid filling system /var/folders)
tmp_cds/

# GAIA runtime
runs/
*.db
*.db-shm
*.db-wal
*.sqlite

# Env / secrets
.env
.env.local
*.pem

# Build
dist/
build/
*.log

# TESS MJO/Gulf download cache (local only)
data/cache/

# Large data (keep locally, not in repo)
data/memory/
data/national_events.json
data/storm_events/

# Large data (keep locally, not in repo)
data/memory/
data/national_events.json
data/storm_events/
tests/fixtures/usgs_earthquakes/earthquakes_2000_2025.csv

# Holler Siren raw DEM cache (derived summaries are committed)
data/holler_siren/raw_dem/
data/holler_siren/raw_dem_western_nc/
data/holler_siren/nlcd_*.tif
data/holler_siren/stage4_*.tif

# Biological layer raw radar cache (derived summaries are committed)
data/bio_signal/nexrad_kpah_morning/

# Proprietary - do not publish
data/holler_siren/model_v5.pkl
data/holler_siren/tfi_model_weights*.json
data/holler_siren/tfi_calibrated*.json
.cdsapirc
*.env
data/cache/
nexrad_*/
WHITEBOARD_*.md
*_CURSOR.md
*_BUILD_*.md
runs/
logs/

# Ferry County Field PWA (Playwright)
ferry_county/frontend/playwright-report/
ferry_county/frontend/test-results/

# Ferry County Electron — PyInstaller output (rebuild with npm run build:backend)
ferry_county/electron/resources/
