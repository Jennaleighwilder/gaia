# GAIA — core web/dashboard dependencies
# Render should install only this file.
# Local scientific/research workflows can add:
#   pip install -r requirements.txt -r requirements-optional.txt

flask>=3.0
gunicorn>=22.0
